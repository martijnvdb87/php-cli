#!/usr/bin/env php
<?php

if(php_sapi_name() !== 'cli') exit;

require __DIR__.'/vendor/autoload.php';

use Martijnvdb\PhpCli\Cli;

$cli = new Cli();

$cli->currentLine("yay")
    ->newLine("yay2")
    ->sleep(1)
    ->clearLine()
    ->sleep(1)
    ->currentLine("yay3")
    ->showCursor();
